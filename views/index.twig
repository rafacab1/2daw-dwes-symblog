{% extends 'layout.twig' %}
{% block body %}
{% for blog in blogs %}
            <article class="blog">
            <div class="date">
            <time datetime=""></time>
            </div>
            <header>
            <h2><a href="show?id={{blog.id}}">{{blog.titulo}}</a></h2>
            </header>
            <img src="{{blog.imagen}}" />
            <div class="snippet">
            <p>{{blog.blog}}</p>
            <p class="continue"><a href="show?id={{blog.id}}">Continue reading...</a></p>
            </div>
            <footer class="meta">
                <p>Comments: <a href="#">{{blog.numComments()}}</a></p>
                <p>Posted by <span class="highlight">{{blog.autor}}</span> at {{blog.created_at.format('Y-m-d H:i:s')}}</p>
                <p>Tags: <span class="highlight">{{blog.tags}}</span></p>
            </footer>
            </article>
{% endfor %}
{% endblock %}